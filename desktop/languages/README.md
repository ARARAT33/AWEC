# AWEC Language Packs

English (`en`) is the base language. Built-in language packs are loaded by the desktop application.

Custom packs use the `.awec.language` format:

```text
language=xx
name=My Language
app.title=...
button.start=...
```

Every visible UI key can be translated or renamed. The desktop language editor should preview each control while editing.
